import type { GameSettings, GameStatistics } from '../game/types'
import { StatisticsPanel } from './StatisticsPanel'
type StartScreenProps = { onStart: () => void; settings: GameSettings; statistics: GameStatistics; onOpenSettings: () => void; onHowToPlay: () => void; onAbout: () => void }
export function StartScreen({ onStart, settings, statistics, onOpenSettings, onHowToPlay, onAbout }: StartScreenProps) {
  return <section className="start-screen"><div className="brand-lockup"><span className="brand-mark" aria-hidden="true">✦</span><span>Orchard Ten</span></div><p className="eyebrow">A SMALL SUM PUZZLE</p><h1>框選數字，<br />湊成 <em>10</em>。</h1><p className="intro">在時間結束前消除最多水果，讓每一次框選都剛剛好。</p><div className="score-snapshot"><span>最高分 <strong>{statistics.highScore}</strong></span><span>上次分數 <strong>{statistics.lastScore}</strong></span></div><button type="button" className="primary-button" onClick={onStart}>開始遊戲 <span aria-hidden="true">→</span></button><div className="start-links"><button type="button" className="text-button" onClick={onHowToPlay}>玩法說明</button><button type="button" className="text-button" onClick={onOpenSettings}>設定 {settings.soundEnabled ? '· 音效開啟' : '· 靜音'}</button><button type="button" className="text-button" onClick={onAbout}>About</button></div><StatisticsPanel statistics={statistics} /><p className="credit-note">核心玩法靈感來自 <a href="https://en.gamesaien.com/game/fruit_box/" target="_blank" rel="noopener noreferrer">Fruit Box</a>，以原創介面與互動重新製作。</p></section>
}
